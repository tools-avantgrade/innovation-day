[phases.setup]
nixPkgs = ["nodejs_18", "npm", "python3", "gcc", "gnumake", "pkg-config"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
